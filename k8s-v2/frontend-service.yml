apiVersion: v1
kind: Service
metadata:
  name: basic3tier-ui-service
  namespace: basic3tier
spec:
  type: NodePort  
  selector:
    app: basic3tier-ui
  ports:
  - name: "frontend-service"
    port: 5000
    targetPort: 80
    nodePort: 30080