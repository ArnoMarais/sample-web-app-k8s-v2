apiVersion: v1
kind: Service
metadata:
  labels:
    app: basic3tier-ui
  name: basic3tier-ui
  namespace: basic3tier
spec:
  type: NodePort
  ports:
  - name: "frontend-service"
    port: 5000
    targetPort: 80
    nodePort: 31000
  selector:
    app: basic3tier-ui